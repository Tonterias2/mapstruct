version: '2'
services:
  mapstruct-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/mapstruct/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=mapstruct
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
